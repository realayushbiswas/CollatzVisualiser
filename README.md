# CollatzVisualiser
A Python visualizer for the Collatz (3x+1) conjecture — plots each number's step-by-step trajectory to 1 with Matplotlib and cycles through consecutive integers automatically. Includes a fixed floating-point precision bug that was silently producing wrong step counts for large starting numbers.
